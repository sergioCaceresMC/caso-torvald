import { defineConfig } from 'astro/config';

export default defineConfig({
  // Actualiza con tu usuario de GitHub antes de hacer deploy:
  // site: 'https://TU_USUARIO.github.io',
  // base: '/linux-course',
  output: 'static',
});
